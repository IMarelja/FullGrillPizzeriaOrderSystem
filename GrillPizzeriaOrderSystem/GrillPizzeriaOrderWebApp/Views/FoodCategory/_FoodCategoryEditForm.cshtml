@model ViewModels.CategoryFoodEditViewModel

<div class="card-body">
    <form asp-controller="FoodCategory" asp-action="Edit">
        @Html.AntiForgeryToken()
        <div class="mb-3">
            <label for="editSelect" class="form-label">Select Category</label>
            <select id="editSelect" class="foodCategoryListDropdown form-select" name="EditFoodCategory">
                <option value="0">Select an Category</option>
            </select>
            <input id="editSelectHiddenId" type="hidden" asp-for="id" class="form-control" />
            <div class="mb-3">
                <label asp-for="name" class="form-label">Name</label>
                <input type="text" asp-for="name" class="form-control" placeholder="Enter new category name" />
            </div>
        </div>
        <div>
            <button type="submit" class="btn btn-warning">
                <i class="bi bi-pencil me-2"></i>Edit
            </button>
        </div>
    </form>
</div>

